// everything inside main

#include <iostream>
using namespace std;
int main()
{
    int length = 10, breadth = 9;
    int area = length * breadth;
    int peri = 2 * (length + breadth);
    cout << area << peri;
    return 0;
}